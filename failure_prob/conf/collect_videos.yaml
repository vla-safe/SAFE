defaults:
  - dataset: openvla
  - model: indep
  - train: base_train
  # See composition order note
  - _self_

train:
  exp_name: "${dataset.name}-${dataset.subset_name}-${train.exp_suffix}"

copy_videos: False
